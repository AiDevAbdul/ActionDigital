# Copy this file to `prisma/.env` so the Prisma CLI can find your database URL.
# Keep this file committed with dummy values so teammates know the variable names.

# Database
DATABASE_URL='postgresql://user:password@host/dbname?sslmode=require'
